/* =====================================================================
   MODELO de configuração.
   Copie este arquivo como "config.js" (na mesma pasta) e preencha com as
   credenciais do seu projeto Supabase: Project Settings → API.

   O config.js real NÃO é versionado no Git (está no .gitignore),
   para a sua chave não vazar caso o repositório seja publicado.
   ===================================================================== */
"use strict";

const SUPA_URL = "https://SEU-PROJETO.supabase.co";
const SUPA_KEY = "SUA_CHAVE_PUBLICA_AQUI";
